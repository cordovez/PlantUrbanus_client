import { createTheme } from "@mui/material/styles";

export const PlantUrbanusTheme = createTheme({
  typography: {
    htmlFontSize: 10,
    fontSize: 10,
  },
  palette: {
    primary: {
      main: "#005048",
    },
    text: {
      primary: "rgba(99,99,99,0.9)",
    },
  },
});
